---
applications:
  - name: tester
    memory: 1G
    instances: 1
    routes:
      - route: preprod.apps.starcommandblue.cf-app.com
    buildpacks:
      - java_buildpack_offline
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ }}'
